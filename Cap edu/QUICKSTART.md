# QazKids - Quick Start Guide for Hackathon

## ⚡ Быстрый старт (30 секунд)

### Способ 1: Docker Compose (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Перейти в директорию проекта
cd "c:\Users\alman\OneDrive\.vscode\Cap edu"

# 2. Запустить все сервисы
docker-compose up -d

# 3. Ждём 30 секунд для инициализации БД

# 4. Открыть в браузере
Frontend:    http://localhost:80
API Docs:    http://localhost:8000/docs
API ReDoc:   http://localhost:8000/redoc
```

**Готово!** 🚀 Всё работает локально.

---

## 📌 Способ 2: Локальный запуск (без Docker)

### Требования
- Python 3.11+
- pip (Python package manager)

### Установка

```bash
# 1. Перейти в backend директорию
cd "c:\Users\alman\OneDrive\.vscode\Cap edu\backend"

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Создать .env файл (если не существует)
copy .env.example .env

# 4. Запустить сервер
uvicorn fastapi_app:app --reload --port 8000

# 5. Открыть браузер
http://localhost:8000/docs
```

---

## 🧪 Тестирование API

### Health Check
```bash
curl http://localhost:8000/health
```

### Регистрация пользователя
```bash
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "test_user",
    "email": "test@example.com",
    "password": "securepassword",
    "full_name": "Test User",
    "age": 12,
    "role": "student"
  }'
```

### Получить список игр
```bash
curl http://localhost:8000/games
```

### Получить список фильмов
```bash
curl http://localhost:8000/films
```

### Войти (Login)
```bash
curl -X POST http://localhost:8000/auth/login \
  -d "email=test@example.com&password=securepassword"
```

---

## 📁 Структура файлов

```
Cap edu/
├── desktop.html                # Главная страница
├── styles.css                  # Стили
├── play/                        # Раздел "Играть"
│   ├── index.html
│   └── game1-3.html
├── cinema/                      # Раздел "Кино-клуб"
│   ├── index.html
│   └── film1-3.html
├── parents/                     # Раздел "Родителям"
│   ├── index.html
│   └── parents-gps.js
├── images/                      # Изображения (SVG)
├── backend/                     # FastAPI Backend
│   ├── fastapi_app.py          # Главное приложение
│   ├── models.py               # SQLAlchemy модели
│   ├── schemas.py              # Pydantic schemas
│   ├── database.py             # БД конфигурация
│   ├── seed.py                 # Инициальные данные
│   ├── requirements.txt         # Python зависимости
│   └── .env.example            # Конфигурация
├── .github/workflows/          # CI/CD
│   └── ci-cd.yml
├── Dockerfile                  # Docker конфигурация
├── docker-compose.yml          # Docker Compose
├── README.md                   # Документация
├── RISK_ANALYSIS.md            # Анализ рисков
├── HACKATHON_CHECKLIST.md      # Финальная проверка
└── QUICKSTART.md               # Этот файл
```

---

## 🔧 Конфигурация

### .env файл (Backend)

```env
# Database
DATABASE_URL=sqlite:///qazkids.db

# Для PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/qazkids

# Security
SECRET_KEY=your-super-secret-key-change-in-production

# API Keys (опционально)
OPENAI_API_KEY=sk-xxx
GOOGLE_MAPS_API_KEY=xxx
```

---

## 🐳 Docker Команды

### Запуск
```bash
docker-compose up -d
```

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f backend
docker-compose logs -f postgres
```

### Перезагрузка
```bash
docker-compose restart
```

### Удаление всех контейнеров
```bash
docker-compose down -v
```

---

## 📊 Что находится где?

| Компонент | URL/Порт | Описание |
|-----------|----------|---------|
| Frontend | http://localhost:80 | Веб-приложение |
| Backend API | http://localhost:8000 | FastAPI сервер |
| API Swagger | http://localhost:8000/docs | Интерактивная документация |
| API ReDoc | http://localhost:8000/redoc | Альтернативная документация |
| PostgreSQL | localhost:5432 | База данных (если Docker) |
| Redis | localhost:6379 | Cache сервис (если Docker) |
| Nginx | localhost:80 | Веб-сервер (если Docker) |

---

## 🧬 Инициализация БД

### Автоматическая (при запуске)
Таблицы создаются автоматически при запуске приложения.

### Добавить пример данных
```bash
cd backend
python seed.py
```

Это добавит:
- ✅ 5 примеров игр
- ✅ 4 примера фильмов
- ✅ 4 примера статей

---

## 🚨 Решение проблем

### Ошибка: "Port already in use"
```bash
# Найти процесс, используемый портом 8000
netstat -ano | findstr :8000

# Убить процесс (замените PID на найденный)
taskkill /PID <PID> /F

# Или использовать другой порт
uvicorn fastapi_app:app --reload --port 8001
```

### Ошибка: "Module not found"
```bash
# Переустановить зависимости
pip install --upgrade -r backend/requirements.txt
```

### Ошибка: "Database connection failed"
```bash
# Убедиться, что PostgreSQL работает (если используется)
# Или использовать SQLite (по умолчанию)
```

### Docker не работает
```bash
# Проверить установку Docker
docker --version
docker-compose --version

# Если не установлен, скачать с https://www.docker.com
```

---

## ✅ Финальная проверка

Перед демонстрацией на хакатоне:

- [ ] Docker Compose запущен (`docker-compose up -d`)
- [ ] Frontend доступен на http://localhost:80
- [ ] API доступен на http://localhost:8000
- [ ] API Swagger docs работают (http://localhost:8000/docs)
- [ ] База данных инициализирована
- [ ] Seed data загружен (опционально)
- [ ] Нет ошибок в логах (`docker-compose logs`)

---

## 🎤 Демонстрационный сценарий

1. **Показать Frontend**
   - Открыть http://localhost:80
   - Пройти по всем разделам (Играть, Кино-клуб, О проекте, Родителям)
   - Показать адаптивный дизайн (F12, responsive mode)

2. **Показать API Documentation**
   - Открыть http://localhost:8000/docs
   - Показать все 25+ endpoints
   - Выполнить примеры запросов (Try it out)

3. **Демонстрация функционала**
   - Регистрация нового пользователя
   - Получение списка игр/фильмов
   - Сохранение прогресса
   - Просмотр GPS координат (родители)

4. **Показать архитектуру**
   - Объяснить backend технологии (FastAPI, SQLAlchemy)
   - Показать Docker Compose (postgres, redis, nginx)
   - Объяснить CI/CD pipeline (GitHub Actions)

5. **Обсудить потенциал**
   - Конкурентные преимущества (казахский язык, локальный контент)
   - Plan масштабирования (мобильные приложения)
   - Финансовая модель (подписки, лицензии)

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте QUICKSTART.md (этот файл)
2. Посмотрите логи: `docker-compose logs`
3. Проверьте requirements.txt установлены
4. Переустановите зависимости: `pip install -r requirements.txt --force-reinstall`

---

## 🎯 Рекомендуемое время демонстрации

- **Setup:** 5 минут (Docker Compose стартует)
- **Frontend demo:** 3 минуты
- **API demo:** 3 минуты
- **Архитектура:** 3 минуты
- **Q&A:** 6 минут
- **Total:** ~20 минут

---

## 📝 Дополнительные ресурсы

- [README.md](README.md) - Полная документация
- [RISK_ANALYSIS.md](RISK_ANALYSIS.md) - Детальный анализ рисков
- [backend/README.md](backend/README.md) - API документация
- [HACKATHON_CHECKLIST.md](HACKATHON_CHECKLIST.md) - Финальная проверка

---

**Версия:** 1.0.0  
**Дата:** December 10, 2025  
**Статус:** ✅ Ready for Hackathon

**Удачи! 🚀**
